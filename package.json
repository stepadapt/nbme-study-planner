{
  "name": "nbme-study-planner",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && node src/index.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
